apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: exercises
  name: pvc-100m
spec:
  storageClassName: pv-100m-exercises
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
